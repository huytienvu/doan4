/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['localhost', 'cdn2.fptshop.com.vn'],// ✅ Cho phép load ảnh từ http://localhost
  },
};

export default nextConfig;
